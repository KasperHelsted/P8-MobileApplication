<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorNewPrimary</item>
        <item name="colorPrimaryDark">@color/colorNewPrimaryDarker</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Toggle Button in day picker -->
    <style name="Widget.Button.Toggle" parent="android:Widget">
        <item name="android:background">@drawable/toggle_bg</item>
    </style>

    <style name="toggleButton"> <!-- removed parent="DefaultTheme"-->
        <item name="android:buttonStyleToggle">@style/Widget.Button.Toggle</item>
        <item name="android:textColor">@drawable/toggle_text_color</item>
    </style>

</resources>
